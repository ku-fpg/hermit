fix-intro
down
unfold-rule "ww"
any-call (unfold '.)
any-call (unfold 'wrap)
one-td (unfold 'unwrap)
any-call (unfold '.)
bash
alpha-let 'work
innermost (bash >+> push 'repH >+> unfold-rule "repH ++" >+> unfold-rule "repH (:)" >+> unfold-rule "rep-abs-fusion" >+> apply-rule "(.) id" >+> unfold-rule "repH []")
-- innermost
-- unfold-rule "(:) ++"
-- unfold-rule "[] ++"
-- unfold-rule "(.) id"