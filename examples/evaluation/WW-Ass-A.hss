-- Worker/Wrapper Assumption A:  wrap (unwrap h) <=> h

--  wrap (unwrap h)
                              { unfold 'wrap }
--  \ e -> unwrap h e Just Nothing
                              { lam-body ; unfold 'unwrap }
--  \ e -> case h e of wild {Nothing -> Nothing ; Just n -> Just n}
                              { any-td (fold 'wild) }
--  \ e -> case h e of wild {Nothing -> wild ; Just n -> wild}
                              { TODO }
--  \ e ->  h e
                              { eta-reduce  }
--  h
